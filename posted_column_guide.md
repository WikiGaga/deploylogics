# posted column guide

## Must add/expose `posted` on these DB objects

1. `vw_purc_grn` (also referenced as `VW_PURC_GRN`)

2. `vw_purc_purchase_order`

3. `vw_purc_stock_dtl` (also referenced as `VW_PURC_STOCK_DTL`)

4. `vw_inve_stock`

